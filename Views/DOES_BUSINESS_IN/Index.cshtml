@model IEnumerable<SEProject.Models.DOES_BUSINESS_IN>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Customer.Customer_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SALES_TERRITORY.Territory_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Customer.Customer_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SALES_TERRITORY.Territory_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DOES_BUSINESS_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DOES_BUSINESS_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DOES_BUSINESS_ID })
        </td>
    </tr>
}

</table>
