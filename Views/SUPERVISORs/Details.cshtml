@model SEProject.Models.SUPERVISOR

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>SUPERVISOR</h4>
    <hr />
    <dl class="dl-horizontal">
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Supervisor_ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
